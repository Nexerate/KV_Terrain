# core pipeline
numpy>=1.24
pyproj>=3.5
rasterio>=1.3
requests>=2.31
scipy>=1.11
# depression hierarchy and water audit (Priority-Flood kernels)
numba>=0.61
# water pipeline (rivers/lakes -> raster masks); affine ships with rasterio
shapely>=2.0
# UI
streamlit>=1.33
folium>=0.16
streamlit-folium>=0.20
# UI extras used directly by ui/ (both ship with streamlit, pinned here because
# this project imports them itself rather than only through streamlit)
pandas>=2.0
pillow>=10.0
